baseURL: "https://loufy03.com/"
title: "LouFY'Log"
theme: ["PaperMod"]

enableGitInfo: true

frontmatter:
  lastmod:
    - lastmod        # ✅ 先用 front matter 里的 lastmod
    - ":git"         # 其次用 Git 的日期
    - date           # 再不行用 date
    - publishDate
    
pagination:
  pagerSize: 20

enableRobotsTXT: true
buildDrafts: false
buildFuture: false
buildExpired: false

outputs:
    home:
      - HTML
      - RSS
      - JSON   # 搜索需要这个

minify:
  disableXML: true
  minifyOutput: true

params:
  env: production
  title: "LouFY'Log"
  description: "my-log"
  keywords: ["Blog", "Notes", "PaperMod"]
  author: "Lou Feiyu"
  DateFormat: "2006-01-02"
  defaultTheme: auto
  disableThemeToggle: false

  ShowReadingTime: true
  ShowShareButtons: false #是否显示分享按钮
  ShowPostNavLinks: false # 文末「上一篇 / 下一篇」
  ShowBreadCrumbs: true # 顶部面包屑导航
  ShowCodeCopyButtons: true    # 代码块右上角复制按钮
  showtoc: true                # 文章侧边目录
  ShowWordCount: true
  ShowRssButtonInSectionTermList: true

  homeInfoParams:
    Title: "👋 Welcome to LouFY'Log"
    Content: >
      Hi, this is LouFY.

  socialIcons:
    - name: github
      url: "https://github.com/Lou-FY"
    - name: rss
      url: "index.xml"

  

menu:
  main:
    - identifier: posts
      name: Posts
      url: /posts/
      weight: 10
    - identifier: archives
      name: Archive
      url: /archives/
      weight: 20
    - identifier: search
      name: Search
      url: /search/
      weight: 30
    - identifier: tags
      name: Tags
      url: /tags/
      weight: 40
    - identifier: faq
      name: FAQ
      url: /faq/
      weight: 50
caches:
  images:
    dir: :cacheDir/images


